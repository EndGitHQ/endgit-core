// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/EndGitHQ/endgit-core/ent/autocheck"
	"github.com/EndGitHQ/endgit-core/ent/build"
	"github.com/EndGitHQ/endgit-core/ent/moderationlog"
	"github.com/EndGitHQ/endgit-core/ent/plugin"
	"github.com/EndGitHQ/endgit-core/ent/pluginanalytics"
	"github.com/EndGitHQ/endgit-core/ent/plugincomment"
	"github.com/EndGitHQ/endgit-core/ent/predicate"
	"github.com/EndGitHQ/endgit-core/ent/rating"
	"github.com/EndGitHQ/endgit-core/ent/report"
	"github.com/EndGitHQ/endgit-core/ent/review"
	"github.com/EndGitHQ/endgit-core/ent/user"
	"github.com/EndGitHQ/endgit-core/ent/version"
)

// PluginQuery is the builder for querying Plugin entities.
type PluginQuery struct {
	config
	ctx                *QueryContext
	order              []plugin.OrderOption
	inters             []Interceptor
	predicates         []predicate.Plugin
	withAuthor         *UserQuery
	withVersions       *VersionQuery
	withBuilds         *BuildQuery
	withReviews        *ReviewQuery
	withReports        *ReportQuery
	withRatings        *RatingQuery
	withComments       *PluginCommentQuery
	withAutoChecks     *AutoCheckQuery
	withModerationLogs *ModerationLogQuery
	withAnalytics      *PluginAnalyticsQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PluginQuery builder.
func (_q *PluginQuery) Where(ps ...predicate.Plugin) *PluginQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *PluginQuery) Limit(limit int) *PluginQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *PluginQuery) Offset(offset int) *PluginQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *PluginQuery) Unique(unique bool) *PluginQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *PluginQuery) Order(o ...plugin.OrderOption) *PluginQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryAuthor chains the current query on the "author" edge.
func (_q *PluginQuery) QueryAuthor() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(plugin.Table, plugin.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, plugin.AuthorTable, plugin.AuthorColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryVersions chains the current query on the "versions" edge.
func (_q *PluginQuery) QueryVersions() *VersionQuery {
	query := (&VersionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(plugin.Table, plugin.FieldID, selector),
			sqlgraph.To(version.Table, version.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, plugin.VersionsTable, plugin.VersionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryBuilds chains the current query on the "builds" edge.
func (_q *PluginQuery) QueryBuilds() *BuildQuery {
	query := (&BuildClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(plugin.Table, plugin.FieldID, selector),
			sqlgraph.To(build.Table, build.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, plugin.BuildsTable, plugin.BuildsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryReviews chains the current query on the "reviews" edge.
func (_q *PluginQuery) QueryReviews() *ReviewQuery {
	query := (&ReviewClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(plugin.Table, plugin.FieldID, selector),
			sqlgraph.To(review.Table, review.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, plugin.ReviewsTable, plugin.ReviewsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryReports chains the current query on the "reports" edge.
func (_q *PluginQuery) QueryReports() *ReportQuery {
	query := (&ReportClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(plugin.Table, plugin.FieldID, selector),
			sqlgraph.To(report.Table, report.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, plugin.ReportsTable, plugin.ReportsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRatings chains the current query on the "ratings" edge.
func (_q *PluginQuery) QueryRatings() *RatingQuery {
	query := (&RatingClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(plugin.Table, plugin.FieldID, selector),
			sqlgraph.To(rating.Table, rating.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, plugin.RatingsTable, plugin.RatingsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryComments chains the current query on the "comments" edge.
func (_q *PluginQuery) QueryComments() *PluginCommentQuery {
	query := (&PluginCommentClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(plugin.Table, plugin.FieldID, selector),
			sqlgraph.To(plugincomment.Table, plugincomment.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, plugin.CommentsTable, plugin.CommentsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAutoChecks chains the current query on the "autoChecks" edge.
func (_q *PluginQuery) QueryAutoChecks() *AutoCheckQuery {
	query := (&AutoCheckClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(plugin.Table, plugin.FieldID, selector),
			sqlgraph.To(autocheck.Table, autocheck.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, plugin.AutoChecksTable, plugin.AutoChecksColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryModerationLogs chains the current query on the "moderationLogs" edge.
func (_q *PluginQuery) QueryModerationLogs() *ModerationLogQuery {
	query := (&ModerationLogClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(plugin.Table, plugin.FieldID, selector),
			sqlgraph.To(moderationlog.Table, moderationlog.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, plugin.ModerationLogsTable, plugin.ModerationLogsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAnalytics chains the current query on the "analytics" edge.
func (_q *PluginQuery) QueryAnalytics() *PluginAnalyticsQuery {
	query := (&PluginAnalyticsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(plugin.Table, plugin.FieldID, selector),
			sqlgraph.To(pluginanalytics.Table, pluginanalytics.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, plugin.AnalyticsTable, plugin.AnalyticsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Plugin entity from the query.
// Returns a *NotFoundError when no Plugin was found.
func (_q *PluginQuery) First(ctx context.Context) (*Plugin, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{plugin.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *PluginQuery) FirstX(ctx context.Context) *Plugin {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Plugin ID from the query.
// Returns a *NotFoundError when no Plugin ID was found.
func (_q *PluginQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{plugin.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *PluginQuery) FirstIDX(ctx context.Context) string {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Plugin entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Plugin entity is found.
// Returns a *NotFoundError when no Plugin entities are found.
func (_q *PluginQuery) Only(ctx context.Context) (*Plugin, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{plugin.Label}
	default:
		return nil, &NotSingularError{plugin.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *PluginQuery) OnlyX(ctx context.Context) *Plugin {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Plugin ID in the query.
// Returns a *NotSingularError when more than one Plugin ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *PluginQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{plugin.Label}
	default:
		err = &NotSingularError{plugin.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *PluginQuery) OnlyIDX(ctx context.Context) string {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Plugins.
func (_q *PluginQuery) All(ctx context.Context) ([]*Plugin, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Plugin, *PluginQuery]()
	return withInterceptors[[]*Plugin](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *PluginQuery) AllX(ctx context.Context) []*Plugin {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Plugin IDs.
func (_q *PluginQuery) IDs(ctx context.Context) (ids []string, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(plugin.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *PluginQuery) IDsX(ctx context.Context) []string {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *PluginQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*PluginQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *PluginQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *PluginQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *PluginQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PluginQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *PluginQuery) Clone() *PluginQuery {
	if _q == nil {
		return nil
	}
	return &PluginQuery{
		config:             _q.config,
		ctx:                _q.ctx.Clone(),
		order:              append([]plugin.OrderOption{}, _q.order...),
		inters:             append([]Interceptor{}, _q.inters...),
		predicates:         append([]predicate.Plugin{}, _q.predicates...),
		withAuthor:         _q.withAuthor.Clone(),
		withVersions:       _q.withVersions.Clone(),
		withBuilds:         _q.withBuilds.Clone(),
		withReviews:        _q.withReviews.Clone(),
		withReports:        _q.withReports.Clone(),
		withRatings:        _q.withRatings.Clone(),
		withComments:       _q.withComments.Clone(),
		withAutoChecks:     _q.withAutoChecks.Clone(),
		withModerationLogs: _q.withModerationLogs.Clone(),
		withAnalytics:      _q.withAnalytics.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithAuthor tells the query-builder to eager-load the nodes that are connected to
// the "author" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PluginQuery) WithAuthor(opts ...func(*UserQuery)) *PluginQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAuthor = query
	return _q
}

// WithVersions tells the query-builder to eager-load the nodes that are connected to
// the "versions" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PluginQuery) WithVersions(opts ...func(*VersionQuery)) *PluginQuery {
	query := (&VersionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withVersions = query
	return _q
}

// WithBuilds tells the query-builder to eager-load the nodes that are connected to
// the "builds" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PluginQuery) WithBuilds(opts ...func(*BuildQuery)) *PluginQuery {
	query := (&BuildClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withBuilds = query
	return _q
}

// WithReviews tells the query-builder to eager-load the nodes that are connected to
// the "reviews" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PluginQuery) WithReviews(opts ...func(*ReviewQuery)) *PluginQuery {
	query := (&ReviewClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withReviews = query
	return _q
}

// WithReports tells the query-builder to eager-load the nodes that are connected to
// the "reports" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PluginQuery) WithReports(opts ...func(*ReportQuery)) *PluginQuery {
	query := (&ReportClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withReports = query
	return _q
}

// WithRatings tells the query-builder to eager-load the nodes that are connected to
// the "ratings" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PluginQuery) WithRatings(opts ...func(*RatingQuery)) *PluginQuery {
	query := (&RatingClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRatings = query
	return _q
}

// WithComments tells the query-builder to eager-load the nodes that are connected to
// the "comments" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PluginQuery) WithComments(opts ...func(*PluginCommentQuery)) *PluginQuery {
	query := (&PluginCommentClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withComments = query
	return _q
}

// WithAutoChecks tells the query-builder to eager-load the nodes that are connected to
// the "autoChecks" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PluginQuery) WithAutoChecks(opts ...func(*AutoCheckQuery)) *PluginQuery {
	query := (&AutoCheckClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAutoChecks = query
	return _q
}

// WithModerationLogs tells the query-builder to eager-load the nodes that are connected to
// the "moderationLogs" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PluginQuery) WithModerationLogs(opts ...func(*ModerationLogQuery)) *PluginQuery {
	query := (&ModerationLogClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withModerationLogs = query
	return _q
}

// WithAnalytics tells the query-builder to eager-load the nodes that are connected to
// the "analytics" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PluginQuery) WithAnalytics(opts ...func(*PluginAnalyticsQuery)) *PluginQuery {
	query := (&PluginAnalyticsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAnalytics = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Plugin.Query().
//		GroupBy(plugin.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *PluginQuery) GroupBy(field string, fields ...string) *PluginGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PluginGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = plugin.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.Plugin.Query().
//		Select(plugin.FieldName).
//		Scan(ctx, &v)
func (_q *PluginQuery) Select(fields ...string) *PluginSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &PluginSelect{PluginQuery: _q}
	sbuild.label = plugin.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PluginSelect configured with the given aggregations.
func (_q *PluginQuery) Aggregate(fns ...AggregateFunc) *PluginSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *PluginQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !plugin.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *PluginQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Plugin, error) {
	var (
		nodes       = []*Plugin{}
		_spec       = _q.querySpec()
		loadedTypes = [10]bool{
			_q.withAuthor != nil,
			_q.withVersions != nil,
			_q.withBuilds != nil,
			_q.withReviews != nil,
			_q.withReports != nil,
			_q.withRatings != nil,
			_q.withComments != nil,
			_q.withAutoChecks != nil,
			_q.withModerationLogs != nil,
			_q.withAnalytics != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Plugin).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Plugin{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withAuthor; query != nil {
		if err := _q.loadAuthor(ctx, query, nodes, nil,
			func(n *Plugin, e *User) { n.Edges.Author = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withVersions; query != nil {
		if err := _q.loadVersions(ctx, query, nodes,
			func(n *Plugin) { n.Edges.Versions = []*Version{} },
			func(n *Plugin, e *Version) { n.Edges.Versions = append(n.Edges.Versions, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withBuilds; query != nil {
		if err := _q.loadBuilds(ctx, query, nodes,
			func(n *Plugin) { n.Edges.Builds = []*Build{} },
			func(n *Plugin, e *Build) { n.Edges.Builds = append(n.Edges.Builds, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withReviews; query != nil {
		if err := _q.loadReviews(ctx, query, nodes,
			func(n *Plugin) { n.Edges.Reviews = []*Review{} },
			func(n *Plugin, e *Review) { n.Edges.Reviews = append(n.Edges.Reviews, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withReports; query != nil {
		if err := _q.loadReports(ctx, query, nodes,
			func(n *Plugin) { n.Edges.Reports = []*Report{} },
			func(n *Plugin, e *Report) { n.Edges.Reports = append(n.Edges.Reports, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withRatings; query != nil {
		if err := _q.loadRatings(ctx, query, nodes,
			func(n *Plugin) { n.Edges.Ratings = []*Rating{} },
			func(n *Plugin, e *Rating) { n.Edges.Ratings = append(n.Edges.Ratings, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withComments; query != nil {
		if err := _q.loadComments(ctx, query, nodes,
			func(n *Plugin) { n.Edges.Comments = []*PluginComment{} },
			func(n *Plugin, e *PluginComment) { n.Edges.Comments = append(n.Edges.Comments, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withAutoChecks; query != nil {
		if err := _q.loadAutoChecks(ctx, query, nodes,
			func(n *Plugin) { n.Edges.AutoChecks = []*AutoCheck{} },
			func(n *Plugin, e *AutoCheck) { n.Edges.AutoChecks = append(n.Edges.AutoChecks, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withModerationLogs; query != nil {
		if err := _q.loadModerationLogs(ctx, query, nodes,
			func(n *Plugin) { n.Edges.ModerationLogs = []*ModerationLog{} },
			func(n *Plugin, e *ModerationLog) { n.Edges.ModerationLogs = append(n.Edges.ModerationLogs, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withAnalytics; query != nil {
		if err := _q.loadAnalytics(ctx, query, nodes,
			func(n *Plugin) { n.Edges.Analytics = []*PluginAnalytics{} },
			func(n *Plugin, e *PluginAnalytics) { n.Edges.Analytics = append(n.Edges.Analytics, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *PluginQuery) loadAuthor(ctx context.Context, query *UserQuery, nodes []*Plugin, init func(*Plugin), assign func(*Plugin, *User)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*Plugin)
	for i := range nodes {
		fk := nodes[i].AuthorId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "authorId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *PluginQuery) loadVersions(ctx context.Context, query *VersionQuery, nodes []*Plugin, init func(*Plugin), assign func(*Plugin, *Version)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Plugin)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(version.FieldPluginId)
	}
	query.Where(predicate.Version(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(plugin.VersionsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PluginId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "pluginId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *PluginQuery) loadBuilds(ctx context.Context, query *BuildQuery, nodes []*Plugin, init func(*Plugin), assign func(*Plugin, *Build)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Plugin)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(build.FieldPluginId)
	}
	query.Where(predicate.Build(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(plugin.BuildsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PluginId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "pluginId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *PluginQuery) loadReviews(ctx context.Context, query *ReviewQuery, nodes []*Plugin, init func(*Plugin), assign func(*Plugin, *Review)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Plugin)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(review.FieldPluginId)
	}
	query.Where(predicate.Review(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(plugin.ReviewsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PluginId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "pluginId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *PluginQuery) loadReports(ctx context.Context, query *ReportQuery, nodes []*Plugin, init func(*Plugin), assign func(*Plugin, *Report)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Plugin)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(report.FieldPluginId)
	}
	query.Where(predicate.Report(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(plugin.ReportsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PluginId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "pluginId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *PluginQuery) loadRatings(ctx context.Context, query *RatingQuery, nodes []*Plugin, init func(*Plugin), assign func(*Plugin, *Rating)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Plugin)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(rating.FieldPluginId)
	}
	query.Where(predicate.Rating(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(plugin.RatingsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PluginId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "pluginId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *PluginQuery) loadComments(ctx context.Context, query *PluginCommentQuery, nodes []*Plugin, init func(*Plugin), assign func(*Plugin, *PluginComment)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Plugin)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(plugincomment.FieldPluginId)
	}
	query.Where(predicate.PluginComment(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(plugin.CommentsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PluginId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "pluginId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *PluginQuery) loadAutoChecks(ctx context.Context, query *AutoCheckQuery, nodes []*Plugin, init func(*Plugin), assign func(*Plugin, *AutoCheck)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Plugin)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(autocheck.FieldPluginId)
	}
	query.Where(predicate.AutoCheck(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(plugin.AutoChecksColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PluginId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "pluginId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *PluginQuery) loadModerationLogs(ctx context.Context, query *ModerationLogQuery, nodes []*Plugin, init func(*Plugin), assign func(*Plugin, *ModerationLog)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Plugin)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(moderationlog.FieldPluginId)
	}
	query.Where(predicate.ModerationLog(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(plugin.ModerationLogsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PluginId
		if fk == nil {
			return fmt.Errorf(`foreign-key "pluginId" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "pluginId" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *PluginQuery) loadAnalytics(ctx context.Context, query *PluginAnalyticsQuery, nodes []*Plugin, init func(*Plugin), assign func(*Plugin, *PluginAnalytics)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Plugin)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(pluginanalytics.FieldPluginId)
	}
	query.Where(predicate.PluginAnalytics(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(plugin.AnalyticsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PluginId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "pluginId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *PluginQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *PluginQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(plugin.Table, plugin.Columns, sqlgraph.NewFieldSpec(plugin.FieldID, field.TypeString))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, plugin.FieldID)
		for i := range fields {
			if fields[i] != plugin.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withAuthor != nil {
			_spec.Node.AddColumnOnce(plugin.FieldAuthorId)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *PluginQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(plugin.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = plugin.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PluginGroupBy is the group-by builder for Plugin entities.
type PluginGroupBy struct {
	selector
	build *PluginQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *PluginGroupBy) Aggregate(fns ...AggregateFunc) *PluginGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *PluginGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PluginQuery, *PluginGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *PluginGroupBy) sqlScan(ctx context.Context, root *PluginQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PluginSelect is the builder for selecting fields of Plugin entities.
type PluginSelect struct {
	*PluginQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *PluginSelect) Aggregate(fns ...AggregateFunc) *PluginSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *PluginSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PluginQuery, *PluginSelect](ctx, _s.PluginQuery, _s, _s.inters, v)
}

func (_s *PluginSelect) sqlScan(ctx context.Context, root *PluginQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
