// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/EndGitHQ/endgit-core/ent/producer"
	"github.com/EndGitHQ/endgit-core/ent/version"
)

// Producer is the model entity for the Producer schema.
type Producer struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// GithubUser holds the value of the "githubUser" field.
	GithubUser string `json:"githubUser,omitempty"`
	// Role holds the value of the "role" field.
	Role producer.Role `json:"role,omitempty"`
	// VersionId holds the value of the "versionId" field.
	VersionId string `json:"versionId,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ProducerQuery when eager-loading is set.
	Edges        ProducerEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ProducerEdges holds the relations/edges for other nodes in the graph.
type ProducerEdges struct {
	// Version holds the value of the version edge.
	Version *Version `json:"version,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// VersionOrErr returns the Version value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ProducerEdges) VersionOrErr() (*Version, error) {
	if e.Version != nil {
		return e.Version, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: version.Label}
	}
	return nil, &NotLoadedError{edge: "version"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Producer) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case producer.FieldID, producer.FieldGithubUser, producer.FieldRole, producer.FieldVersionId:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Producer fields.
func (_m *Producer) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case producer.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case producer.FieldGithubUser:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field githubUser", values[i])
			} else if value.Valid {
				_m.GithubUser = value.String
			}
		case producer.FieldRole:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field role", values[i])
			} else if value.Valid {
				_m.Role = producer.Role(value.String)
			}
		case producer.FieldVersionId:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field versionId", values[i])
			} else if value.Valid {
				_m.VersionId = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Producer.
// This includes values selected through modifiers, order, etc.
func (_m *Producer) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryVersion queries the "version" edge of the Producer entity.
func (_m *Producer) QueryVersion() *VersionQuery {
	return NewProducerClient(_m.config).QueryVersion(_m)
}

// Update returns a builder for updating this Producer.
// Note that you need to call Producer.Unwrap() before calling this method if this Producer
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Producer) Update() *ProducerUpdateOne {
	return NewProducerClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Producer entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Producer) Unwrap() *Producer {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Producer is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Producer) String() string {
	var builder strings.Builder
	builder.WriteString("Producer(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("githubUser=")
	builder.WriteString(_m.GithubUser)
	builder.WriteString(", ")
	builder.WriteString("role=")
	builder.WriteString(fmt.Sprintf("%v", _m.Role))
	builder.WriteString(", ")
	builder.WriteString("versionId=")
	builder.WriteString(_m.VersionId)
	builder.WriteByte(')')
	return builder.String()
}

// Producers is a parsable slice of Producer.
type Producers []*Producer
